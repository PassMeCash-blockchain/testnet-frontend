import { MyWallets, Home2, notify, Request, PayBills, Transactions, user, VirtualCard } from '/assets/';

export const Images = {
    MyWallets, Home2, notify, Request, PayBills, Transactions, user, VirtualCard
}

